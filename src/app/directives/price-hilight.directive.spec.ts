import { PriceHilightDirective } from './price-hilight.directive';

describe('PriceHilightDirective', () => {
  it('should create an instance', () => {
    const directive = new PriceHilightDirective();
    expect(directive).toBeTruthy();
  });
});
